# Prototype

This directory contains the original HTML prototype/mockup of the task tracker interface.

## Files

- `index.html` - Original static HTML prototype showing the basic UI concept

This prototype was used as a reference during development but is not part of the final React Native/Expo implementation.

